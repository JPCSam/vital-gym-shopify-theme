<div class="offers">
   <div class="page-width">
      <div class="offers__wrapper">
         <div class="offers__image">
            <div class="offers__image-desktop">
               <img src="{{ section.settings.image_desktop | img_url: "x632" }}" alt="{{ section.settings.image_desktop.alt }}">
            </div>
            <div class="offers__image-mobile">
               <img src="{{ section.settings.image_mobile | img_url: "x420" }}" alt="{{ section.settings.image_mobile.alt }}">
            </div>
         </div>
         <div class="offers__info">
            {%- capture highlighted -%}
               <span>{{- section.settings.section_title_highlighted -}}</span>
            {%- endcapture -%}
            <div class="offers__info-title">{{ section.settings.section_title | replace: section.settings.section_title_highlighted, highlighted }}</div>
            <div class="offers__info-description">{{ section.settings.description }}</div>
         </div>
      </div>
   </div>
</div>

{% schema %}
   {
      "name": "Offers",
      "settings": [
         {
            "type": "header",
            "content": "Section header"
         },
         {
            "type": "text",
            "id": "section_title",
            "label": "Title",
            "default": "Exclusive offers"
         },
         {
            "type": "text",
            "id": "section_title_highlighted",
            "label": "Highlighted word",
            "default": "offers"
         },
         {
            "type": "richtext",
            "id": "description",
            "label": "Description",
            "default": "<p>You can get huge discounts (up to 70% off) on Vital Gym products so you can kit out your home gym for less.</p><p>Plus, you’ll get early access on our new products so you can grab those dumbbells before they sell out again!</p><p>And if that’s not enough - you’ll also go in the draw to win a free prize worth up to £100 every month.</p>"
         },
         {
            "type": "image_picker",
            "id": "image_desktop",
            "label": "Image for desktop"
         },
         {
            "type": "image_picker",
            "id": "image_mobile",
            "label": "Image for mobile"
         } 
      ],
      "presets": [
         {
            "category": "Vital Club",
            "name":"Offers"
         }
      ]
   }
{% endschema %}